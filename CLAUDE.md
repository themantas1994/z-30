# CLAUDE.md

The working context for this repository — architecture, generated files, the safety invariants
that have tests behind them, and the house rules — lives in `AGENTS.md`, so that every coding
assistant reads the same thing.

@AGENTS.md
